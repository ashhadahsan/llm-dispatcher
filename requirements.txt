# Runtime dependencies for llm-dispatcher.
# This is the core install. For optional features, install extras via:
#   pip install 'llm-dispatcher[image]'           # Pillow
#   pip install 'llm-dispatcher[audio]'           # librosa, soundfile, pydub
#   pip install 'llm-dispatcher[multimodal]'      # image + audio
#   pip install 'llm-dispatcher[semantic-cache]'  # sentence-transformers, faiss
#   pip install 'llm-dispatcher[evaluation]'      # ragas, langchain, sklearn
#   pip install 'llm-dispatcher[reports]'         # matplotlib, seaborn, plotly
#   pip install 'llm-dispatcher[benchmark]'       # transformers, torch, accelerate
#   pip install 'llm-dispatcher[all]'             # everything above
#   pip install 'llm-dispatcher[dev]'             # test + lint tooling
#   pip install 'llm-dispatcher[docs]'            # mkdocs

pydantic>=2.0.0
openai>=1.0.0
anthropic>=0.7.0
google-generativeai>=0.3.0
requests>=2.31.0
tiktoken>=0.5.0
numpy>=1.21.0
pyyaml>=6.0
python-dotenv>=1.0.0
